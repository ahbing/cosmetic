extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each product in products
      li
        a(href="#") #{product.productTitle + product.productId} &nbsp;&nbsp;| 
        a(href="/delete/"+ product.productId) 删除